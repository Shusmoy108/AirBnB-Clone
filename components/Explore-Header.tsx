import { StyleSheet, Text, View } from "react-native";

const ExploreHeader = () => {
	return (
		<View>
			<Text>Explore Header</Text>
		</View>
	);
};

export default ExploreHeader;

const styles = StyleSheet.create({});
